<prism:VisualStateAwarePage
    x:Class="Eqstra.ServiceScheduling.WindowsPhone.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eqstra.ServiceScheduling.WindowsPhone.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="using:Microsoft.Practices.Prism.StoreApps"
    mc:Ignorable="d"
    
    xmlns:mvvm="using:Microsoft.Practices.Prism.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    >
    <prism:VisualStateAwarePage.Background>
        <ImageBrush ImageSource="ms-appx:///Assets/login_bg.jpg" Stretch="UniformToFill"/>
    </prism:VisualStateAwarePage.Background>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0">
            <Image Source="ms-appx:///Assets/Eqstra_headerlogo.png" Stretch="None"/>
            <TextBox PlaceholderText="User Name" Background="{ThemeResource PhoneAccentBrush}" Height="45" Width="350" Text="{Binding UserName,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox PlaceholderText="Password" Background="{ThemeResource PhoneAccentBrush}"  Height="45" Width="350" Password="{Binding Password,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button HorizontalAlignment="Center" Style="{StaticResource LoginButtonStyle}" Command="{Binding LoginCommand}" >
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="ms-appx:///Assets/loginIcon.png" Width="50" Height="50"/>
                        <TextBlock Text="Login" Margin="20 15"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</prism:VisualStateAwarePage>
