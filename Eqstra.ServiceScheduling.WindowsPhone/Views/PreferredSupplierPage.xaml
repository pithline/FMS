<prism:VisualStateAwarePage
    x:Class="Eqstra.ServiceScheduling.WindowsPhone.Views.PreferredSupplierPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eqstra.ServiceScheduling.WindowsPhone.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:prism="using:Microsoft.Practices.Prism.StoreApps"
    xmlns:mvvm="using:Microsoft.Practices.Prism.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <prism:VisualStateAwarePage.Resources>

    </prism:VisualStateAwarePage.Resources>


    <prism:VisualStateAwarePage.BottomAppBar>
        <CommandBar Visibility="{Binding AppBarVisibility}">
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Filter" Icon="Filter">
                    <AppBarButton.Flyout>
                        <Flyout>
                            <StackPanel>
                                <TextBox FontSize="25"/>
                            </StackPanel>
                        </Flyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
                <AppBarButton Label="Find" Icon="Find" Name="Find" Click="Find_Click"/>
                <AppBarButton Label="Submit" Icon="Accept"/>
            </CommandBar.PrimaryCommands>
            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Calendar" Icon="Calendar"/>
                <AppBarButton Label="Profile" Icon="NewWindow" Name="Profile" Click="More_Click"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </prism:VisualStateAwarePage.BottomAppBar>

    <Grid>

        <ListView HorizontalAlignment="Center"  ScrollViewer.VerticalScrollBarVisibility="Visible"
                      VerticalAlignment="Center" 
                      ItemsSource="{Binding PoolofSupplier}" ItemContainerStyle="{StaticResource ListViewItemStyle1}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                            HorizontalAlignment="Center"
                            Foreground="White" 
                            TextAlignment="Center" 
                            Text="{Binding SupplierName}" Style="{StaticResource InfoBoxTitleStyle}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>

</prism:VisualStateAwarePage>
