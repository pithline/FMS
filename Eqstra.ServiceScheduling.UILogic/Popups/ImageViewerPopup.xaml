<Page
    x:Class="Eqstra.ServiceScheduling.UILogic.Popups.ImageViewerPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eqstra.ServiceScheduling.UILogic.Popups"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button x:Name="backButton" VerticalAlignment="Top" Click="Close_Click" HorizontalAlignment="Left"
                        Style="{StaticResource NavigationBackButtonNormalStyle}" Background="LightGoldenrodYellow"
                        Margin="10 0 0 20"/>
            <Image Grid.Row="1" x:Name="Viewer" Source="{Binding ImagePath}" Stretch="None" Margin="10">
                
            </Image>        </Grid>

    </Grid>
</Page>
